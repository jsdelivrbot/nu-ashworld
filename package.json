{
  "license": "UNLICENSED",
  "scripts": {
    "build:client": "node_modules/.bin/elm make --optimize src/Client/Main.elm --output dist/elm-client.js",
    "build:client:debug": "node_modules/.bin/elm make --debug src/Client/Main.elm --output dist/elm-client.js",
    "build:server": "node_modules/.bin/elm make --optimize src/Server/Main.elm --output dist/elm-server.js",
    "build:server:debug": "node_modules/.bin/elm make --debug src/Server/Main.elm --output dist/elm-server.js",
    "build": "yarn build:client && yarn build:server",
    "build:debug": "yarn build:client:debug && yarn build:server:debug",
    "format": "node_modules/.bin/elm-format --yes src",
    "start:client": "yarn build:client && cd dist && live-server --port=4444 --watch=index.html,elm-client.js",
    "start:server": "yarn build:server && cd dist && nodemon -w server.js -w elm-server.js server.js",
    "start": "yarn build && node dist/server.js"
  },
  "main": "dist/server.js",
  "engines": {
    "node": "10.6.0"
  },
  "dependencies": {
    "express": "^4.16.3",
    "live-server": "^1.2.0",
    "nodemon": "^1.18.4"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.0"
  }
}
