{
  "scripts": {
    "build:client": "elm make --optimize client/src/Main.elm",
    "build:client:debug": "elm make --debug client/src/Main.elm",
    "format": "elm-format --yes client"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.0"
  }
}
